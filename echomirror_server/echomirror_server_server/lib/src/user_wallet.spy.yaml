class: UserWallet
table: user_wallets

fields:
  userId: int
  echoBalance: double, default=0
  stellarPublicKey: String?
  createdAt: DateTime, default=now
  updatedAt: DateTime, default=now

indexes:
  user_wallet_user_idx:
    fields: userId
    unique: true
