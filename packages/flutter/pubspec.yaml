name: echomirror_flutter
description: "EchoMirror Flutter plugin package - shared utilities and widgets for EchoMirror SDK."
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.8.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true